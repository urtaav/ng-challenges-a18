import { NgforEmptyDirectiveDirective } from './ngfor-empty-directive.directive';

describe('NgforEmptyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NgforEmptyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
